import React from "react";
import { Landing } from "./views"; // ToDo: refactor into routes with react-router when there is more than 1 view

function App() {
  return <Landing />;
}

export default App;
